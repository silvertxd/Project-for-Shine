<UserControl x:Class="Project.MVVM.View.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:Project.MVVM.ViewModel"
             xmlns:local="clr-namespace:Project.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:ExportViewModel/>
    </UserControl.DataContext>
    <StackPanel>

        <Button Width="50" Height="50" Content="123" Command="{Binding TotalSalesCountCommand}"/>
        <Button Width="50" Height="50" Content="333" Command="{Binding CountBySellerCommand}"/>
    </StackPanel>
</UserControl>
